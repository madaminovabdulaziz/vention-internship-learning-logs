Generator Expressions note: I have learned that using generator expressions is useful when we need to save Memory. 
because, when we use usual for loop, it loads everything as a whole to memory, but, when using generators, it only loads on demand
